---
id: task-0002
title: Baseline houseCARL conflict/load-order scan of current Anvil-based list
assigned_to: claude-code
requires_housecarl: true
status: done
created: 2026-07-05
depends_on:
---

## Description

Before any customization work is delegated to Cursor for research/drafting,
establish a clean baseline of the currently installed Anvil-based load
order via houseCARL. This is the reference point every later task and
decision-log entry gets compared against.

## Acceptance criteria

- [x] Confirm houseCARL resolves the correct MO2 instance (check the
      `load_order_status` header names the right instance and profile) —
      original pass surfaced a discrepancy with CLAUDE.md by resolving
      E:\Modlists\Skyrim AE; rerun repointed houseCARL at
      `D:\Skyrim AI Modlist\Anvil` (user-confirmed correct, matches
      CLAUDE.md), see Result
- [x] Produce a full plugin list in current load order, with count
- [x] Run a conflict-tree pass across the load order and list every
      record-level conflict found, noting which plugin currently wins
      each one — **scope note:** 129,515 total conflicts (rerun, D:
      instance) is too large to list individually and stay scannable (see
      next item); did per-type aggregate counts for the ~20 largest
      categories plus a full line-by-line audit of 4 smaller categories
      (327 records). See Result and `baseline-scan-2026-07-05.md` for the
      full scoping rationale.
- [x] Flag any conflicts that look unintentional or unresolved (i.e. not
      already covered by an existing patch or known compatibility fix) —
      none found in the 327 individually-audited records; large categories
      not yet individually checked (see follow-ups)
- [x] Note any plugins with missing masters or other load-order errors
- [x] Write the results to `modlist/baseline-scan-2026-07-05.md`
      (plugin list + conflict summary; keep it scannable, not a raw dump)
- [x] Add a summary entry to `modlist/decisions.md` referencing the new
      baseline file
- [x] If any conflicts found look like known/expected issues from prior
      Wabbajack/Anvil documentation, note that in the write-up rather than
      treating them as new problems — no prior documentation existed
      locally to check against; noted as a gap instead

## Result

**Rerun (2026-07-05):** The original pass below ran against houseCARL's
then-configured instance, `E:\Modlists\Skyrim AE`, which was flagged as a
discrepancy against CLAUDE.md rather than treated as an error. The user
confirmed `D:\Skyrim AI Modlist\Anvil` — the path CLAUDE.md already
documented — is the correct instance (a baseline list actively being added
to). houseCARL was repointed at it via `housecarl_set_mo2_instance`
(persists across restarts) and the full scan was redone against it.

Corrected findings:
- Confirmed instance/profile: `D:\Skyrim AI Modlist\Anvil`, profile
  `Anvil - Main Profile` — matches CLAUDE.md exactly, no discrepancy.
- Full plugin list (354 active plugins: 924 mods enabled / 9 disabled)
  regenerated and written to `modlist/baseline-plugins-2026-07-05.txt`.
- Ran conflict-tree pass: 129,515 total record-level conflicts. Got
  per-type counts for the ~20 biggest record types (~31,377 of the total),
  and did a full manual audit of every conflict in four smaller categories
  (Race, Faction, Class, Worldspace — 327 records). No unintentional/
  unresolved conflicts found; every winner is USSEP/USMP, a DLC/master
  override, a named compatibility patch, or expected generated-tool output.
- 0 missing masters, 0 excluded/unparseable plugins (cleaner than the
  original E: pass, which had 3 — that finding does not apply here).
- Rewrote `modlist/baseline-scan-2026-07-05.md` in place and replaced the
  decisions.md entry to reflect the corrected instance; the E: numbers are
  superseded and should not be referenced going forward.

**Files changed (rerun):**
- Overwrote `modlist/baseline-scan-2026-07-05.md`
- Overwrote `modlist/baseline-plugins-2026-07-05.txt`
- Updated `modlist/decisions.md` (replaced the original entry)

**Follow-up tasks implied (not created yet — for Cursor/human review):**
1. Decide whether this list needs a Bashed/Smashed Patch (currently
   absent — `load-order-notes.md` documents "bashed/smashed last" as
   methodology but no such plugin exists; load order ends with Synthesis
   patchers/ParallaxGen/DynDOLOD/Occlusion instead). Unchanged from the
   original pass.
2. If deeper conflict resolution is wanted, follow-up tasks per record
   type for the large, not-yet-individually-audited categories (Cell,
   NPC_, dialogue, Armor, Weapon, ConstructibleObject, LeveledItem,
   MagicEffect, Spell, Quest, Perk).

---

**Original pass (superseded — ran against the wrong instance, kept for
history):**

- Confirmed instance/profile houseCARL resolved: `E:\Modlists\Skyrim AE`,
  profile `Anvil - Main Profile` — flagged as not matching CLAUDE.md's
  documented `D:\Skyrim AI Modlist\Anvil` path; treated E: as authoritative
  at the time. This was later determined to be the wrong call — D: is the
  correct instance (see rerun above).
- Full plugin list (4,204 active plugins: 3,974 mods enabled / 190
  disabled) — see superseded `modlist/baseline-plugins-2026-07-05.txt`
  content (now overwritten).
- Conflict-tree pass: 349,185 total record-level conflicts, 428 records
  individually audited (Race/Faction/Class/Worldspace), all healthy.
- 0 missing masters. 3 plugins excluded for unparseable records:
  `DarkEnvoy.esp`, `CurseOfTheHoundAmulet - Fixes.esp`, `ksws03_quest.esp`
  — these are E:-instance-specific findings and do not apply to D:.
