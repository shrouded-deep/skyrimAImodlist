---
id: task-0061
title: Create Outputs category; move LOD outputs to highest priority
assigned_to: claude-code
requires_housecarl: false
expected_mo2_instance: Keizaal - Fork
status: done
created: 2026-07-12
completed: 2026-07-12
depends_on: task-0058
---

## Description

Keizaal ships its generated LOD outputs (`DynDOLOD Output`, `TexGen Output`, `xLODGEN`)
near the BOTTOM of `modlist.txt` = LOWEST asset priority. After task-0058 moved our
additions under `Uncategorized` (top of file = HIGHEST priority), the additions began
overriding the outputs' loose LOD files. User caught it and directed a dedicated
`Outputs` category at highest priority.

**Priority direction confirmed:** top of `modlist.txt` = HIGHEST priority (base-game
separator sits at the file bottom). Earlier task-0058 docs stated this backwards; corrected.

## What was done

- Backup: `profiles/Keizaal - Fork/modlist.backup-outputs-2026-07-12.txt`
- Classified the LOD stack: `DynDOLOD Output`, `TexGen Output`, `xLODGEN` are generated
  OUTPUTS (meshes/textures/lod). `DynDOLOD DLL` (SKSE) and `DynDOLOD` (Resources) are
  runtime INPUTS — left in place.
- Created `+Outputs_separator` at the very top of the file; moved the 3 outputs above it
  (order: DynDOLOD Output, TexGen Output, xLODGEN — highest→lowest), so they sit ABOVE
  the Uncategorized additions and win all asset conflicts.
- Safety-checked: 925 → 926 lines (+1 separator), 903 mod lines preserved, 0 duplicates.

Only `modlist.txt` (asset priority) changed. `plugins.txt` / `loadorder.txt` untouched.

## Result

Done. Requires user F5 + confirm the `Outputs` group renders at the correct end of the UI.
**NOTE:** priority is now correct but the outputs are STALE (see task-0060 regen) — the
new city buildings still lack generated LOD until a regen pass. Priority fix is prep.
