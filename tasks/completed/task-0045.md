---
id: task-0045
title: JK/Ryn stack - finish installs after Nexus downloads
assigned_to: cursor
requires_housecarl: false
status: cancelled
created: 2026-07-08
depends_on: task-0043
---

**Cancelled 2026-07-11:** Anvil bottom-up approach abandoned per pivot decision
(`modlist/decisions.md`). JK/Ryn stack install no longer pursued on Anvil donor.
See `task-0046` (Keizaal fork-test) for replacement direction.

---

## Description

task-0043 partial completion: `scripts/install-jk-ryn-stack.ps1` written and
run with MO2 closed. **0/88 Nexus archives** were in `Anvil/Downloads/` - no
mods installed. MAST unchanged at 0 (408 plugins).

After user downloads all archives via MO2, re-run:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/install-jk-ryn-stack.ps1
```

Script is idempotent. Full missing-ID inventory is in `tasks/queue/task-0043.md`
Result section.

## Acceptance criteria

- [ ] All 88 archives from task-0043 inventory present in `Anvil/Downloads/`
- [ ] Re-run `scripts/install-jk-ryn-stack.ps1` - exit 0, MAST 0
- [ ] All JK/Ryn mods enabled in MO2 + plugins active per task-0043 criteria
- [ ] 78920 / 87964 / 91642 / 95750 hub patches applied per stack defaults
- [ ] UME JK Understone patches enabled once JK Understone Keep installed
- [ ] **Lux Patch Hub (113002) FOMOD re-run** after all JK/Ryn mods installed - pick up any JK interior patches (same pattern as city stack pass in task-0042). Check hub for coverage of at minimum: JK's Dragonsreach, Jorrvaskr, Blue Palace, Candlehearth Hall, and any other JK interiors present in the FOMOD options.
- [ ] All new mod folders in `[Successor Additions]` zone
- [ ] task-0043 moved to completed; Result filled in

## Result

### Status: **PARTIAL FAILURE (2026-07-08 user re-run)**

MO2 closed (`Assert-Mo2Closed` OK). User reported downloads complete; preflight scan run before install.

### Archive inventory (`Anvil/downloads/`)

| Metric | Value |
|---|---|
| **Task inventory** | **88** mods |
| **Script unique Nexus IDs** | **87** (inventory counts one mod twice vs script list) |
| **Archives present (script ID match)** | **86 / 87** |
| **Missing download** | **62219** (JK's High Hrothgar) |

All other task-0043 Nexus IDs had a matching `-{id}-` archive in Downloads at preflight time.

### Script run (requested)

```powershell
powershell -ExecutionPolicy Bypass -File scripts/install-jk-ryn-stack.ps1
```

| Item | Value |
|---|---|
| **Exit code** | **1** (unhandled throw — extract verification) |
| **Failure point** | **JK's Whiterun Outskirts (78351)** after 41 prior mods extracted |
| **Cause** | `Install-ModFromArchive` ESP verify: archive yields `JK's Whiterun's Outskirts.esp` but `EspLike` is `*Whiterun Outskirts*` (apostrophe in `Whiterun's` breaks wildcard match) |
| **Mods installed this run** | **41** mod folders (+ partial **JK's Whiterun Outskirts** folder with .esp/.bsa on disk) |
| **modlist.txt / Successor Additions** | **unchanged** (script aborted before `Ensure-SuccessorModlistBlock`) |
| **MAST before** | **0** missing masters (**408** active plugins) |
| **MAST after (main run)** | **unchanged** — no plugin enable pass ran |
| **Log** | `modlist/exports/install-jk-ryn-run.log` |

### Side note (load order hygiene)

An accidental local `-SkipInstall` probe after the failed run briefly enabled JK plugins (including `.bsa` lines) in `plugins.txt`; those starred lines were **removed** so **408** active plugins again. **`loadorder.txt` still contains 42 JK-related lines** inserted after `Lux.esp` from that probe — remove or re-run a clean install pass after script fix.

### Lux Patch Hub (113002)

**Not handled by `install-jk-ryn-stack.ps1`.** After a successful full pass (exit 0, MAST 0), **manual MO2 FOMOD re-run on Lux Patch Hub** required per acceptance criteria.

### User / agent actions to unblock

1. Keep **MO2 closed** before any script re-run.
2. Download **62219** (JK's High Hrothgar) if still missing.
3. Fix script `EspLike` for **78351** (e.g. `*Whiterun*Outskirts*`) or install JK Whiterun Outskirts via MO2 FOMOD and adjust script to skip/reconcile.
4. Re-run: `powershell -ExecutionPolicy Bypass -File scripts/install-jk-ryn-stack.ps1` (idempotent for already-installed folders).
5. On success: MO2 **F5**; clean **loadorder.txt** if stale JK lines remain; **Lux Patch Hub (113002) FOMOD**; verify MAST 0 and `[Successor Additions]`.

**Tasks 0043 and 0045 remain in `tasks/queue/`** until exit 0, MAST 0, and acceptance criteria met.
