---
id: task-0057
title: Restore omitted CC content into a standalone mod (satisfies Nolvus PoS patches)
assigned_to: claude-code
requires_housecarl: true
expected_mo2_instance: Keizaal - Fork
status: done
completed: 2026-07-12
created: 2026-07-12
depends_on: task-0050
---

## Description

The VB stack (copied from Nolvus) includes Path of Sorcery patches for 3 Creation Club
creations that Keizaal curates OUT, leaving missing masters that would CTD at load
(user spotted the MO2 warning icons). Rather than disable the patches, **restore the
default CC content** so the patches work and the player gets that content — while
keeping Keizaal's own mods pristine (new standalone mod, no edits to list mods).

**Methodology correction (keep):** houseCARL's `load_order_status` summary did NOT catch
these missing ESL-master references — it checks that plugin *files* exist but tolerates
missing master references silently. Use `scripts/full-mast-scan.ps1` (reads each active
plugin's master list vs what's present) for MAST audits. Do not rely on the houseCARL
summary alone.

## Source (user-owned AE content — verified present)

`D:\Steam\steamapps\common\Skyrim Special Edition\Data`:
- `ccbgssse014-spellpack01.esl` + `.bsa` — Arcane Accessories
- `cckrtsse001_altar.esl` + `.bsa` — Bittercup
- `ccvsvsse003-necroarts.esl` + `.bsa` — Necromantic Grimoire

## Plan

1. New MO2 mod folder `Restored CC - Arcane Accessories, Bittercup, Necromantic Grimoire`
   in `E:\Skyrim\mods\` containing the 3 `.esl` + 3 `.bsa` (all together, one mod).
2. Enable it in modlist.txt inside the `[Keizaal Additions]` zone (high priority — if
   anything ever conflicts, our restore wins; "reverts to default CC state, stays pristine").
3. Activate the 3 ESLs in plugins.txt + place in loadorder.txt in the ESL/master block.
4. Keep the 5 PoS CC patches ENABLED (now their masters resolve):
   PoS_ArcaneAccessories_Patch, PoS_Bittercup_Patch, PoS_NecromanticGrimoire_Patch,
   PoS_CreationClub_LList_Patch, DISCO_PathOfSorcery_Mashup.
5. **DISABLE only `DISCO_VokriinatorISCPatch.esp`** — it needs Immersive Sounds Compendium
   (a sound overhaul, NOT CC; Keizaal doesn't use it). Not in scope to restore.

## Acceptance criteria

- [ ] New mod created with the 6 files; enabled in `[Keizaal Additions]`
- [ ] 3 CC ESLs active; 5 PoS patches remain active with masters satisfied
- [ ] `DISCO_VokriinatorISCPatch.esp` disabled (ISC not restored)
- [ ] `scripts/full-mast-scan.ps1`: 0 active plugins with missing masters
- [ ] Record new active + full-weight counts (ESLs don't change full-weight)
- [ ] Note: this re-adds gameplay content Keizaal cut (Bittercup quest, Necromancy
      spells, Arcane staves) — deliberate. Watch for TD/leveled-list oddities in smoke test.
- [ ] Update decisions.md

## Result

**Status: COMPLETE — 2026-07-12. Load order clean, 0 missing masters.**

Restored the 3 omitted CC creations into a standalone mod so the Nolvus PoS patches
resolve, keeping Keizaal's own mods pristine.

### Done

- New mod `Restored CC - Arcane Accessories, Bittercup, Necromantic Grimoire` in
  `E:\Skyrim\mods\` with the 3 `.esl` + 3 `.bsa` (copied from the user's AE install
  `D:\Steam\steamapps\common\Skyrim Special Edition\Data`). MO2 auto-registered it at the
  top of modlist.txt (highest priority) on close — enabled it there ("overwrites his
  plugins" / default CC state).
- 3 CC ESLs inserted into loadorder.txt master block; force-loaded as implicit masters
  (houseCARL implicit masters 54 → 57). Kept out of plugins.txt, matching Keizaal's CC
  convention.
- 5 PoS CC patches kept ACTIVE, now with masters satisfied.
- `DISCO_VokriinatorISCPatch.esp` DISABLED (needs Immersive Sounds Compendium — a sound
  overhaul, not CC; out of scope).

### Verification

- `scripts/full-mast-scan.ps1` (updated to treat enabled-mod masters as present):
  **0 active plugins with missing masters.**
- houseCARL: **824 active, 824/824 resolved, no warnings.** 3 CC recognized as implicit
  masters.
- Full-weight vs 254 cap: unchanged (~104; CC restores are ESL, one ESP patch disabled) —
  ample headroom.

### Content note

This re-adds gameplay Keizaal cut: Bittercup (quest/item), Necromantic Grimoire
(necromancy spells + minor questline), Arcane Accessories (staves/items) — all now
PoS-balanced. Watch for TD/leveled-list oddities in the VB smoke test.

### Methodology

houseCARL's `load_order_status` summary did NOT catch the original missing ESL-master
references (it verifies plugin files exist, tolerates missing master refs). Use
`scripts/full-mast-scan.ps1` for MAST audits. User caught this via MO2's warning icons.
