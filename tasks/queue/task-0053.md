---
id: task-0053
title: Source correct modular CWE × Spaghetti's Whiterun patch (or confirm none needed)
assigned_to: cursor
requires_housecarl: false
expected_mo2_instance: Keizaal - Fork
status: queued
created: 2026-07-12
depends_on: task-0052
---

## Description

task-0052 disabled `CWE Spaghetti Patch` on `Keizaal - Fork` — the installed plugin
(`Captial Whiterun and Spaghetti AIO Patch.esp`) masters `Spaghetti's Cities - AIO.esp`,
which is not active (we use the modular Spaghetti ESPs). It was the wrong FOMOD variant.

Capital Whiterun Expansion (`SurWR.esp`) and `Spaghetti's Cities - Whiterun.esp` both
edit the Whiterun exterior and currently have no reconciling patch — one silently
overrides the other by load order.

## Acceptance criteria

- [ ] Check Nexus mod 116487 (CWE × Spaghetti) FOMOD for a variant that masters
      `Spaghetti's Cities - Whiterun.esp` (modular) rather than the AIO plugin
- [ ] If a modular variant exists: install it, enable on `Keizaal - Fork`, verify it
      masters only active plugins (0 missing masters via houseCARL)
- [ ] If only an AIO variant exists: determine whether a CWE+modular-Whiterun conflict
      is material (compare exterior cell edits in xEdit/houseCARL). If minor, document
      that no patch is needed and set load order so CWE wins (or Spaghetti wins per
      preference). If major, flag for a hand-authored patch decision.
- [ ] Record outcome; update decisions.md if a patch is added or a conflict is accepted

## Result

(Pending.)
