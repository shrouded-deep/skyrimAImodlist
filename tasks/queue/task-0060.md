---
id: task-0060
title: Tier-2 regen after content additions (xLODGen/TexGen/DynDOLOD + grass cache)
assigned_to: human
requires_housecarl: false
expected_mo2_instance: Keizaal - Fork
status: deferred
created: 2026-07-12
depends_on: task-0059
---

## Description

The VB stack (task-0050), city expansions (task-0051), and restored CC (task-0057) added
worldspace buildings and objects that the existing generated LOD does not cover. The
`DynDOLOD Output` / `TexGen Output` / `xLODGEN` outputs are STALE — the new city buildings
(Capital Whiterun/Windhelm, Ultimate Markarth, RedBag's Solitude, City of Crossed Daggers,
Riverwood Has Walls) currently have no distant LOD. task-0059 fixed their priority; this
task refreshes their content.

**Deferred by user (2026-07-12).** Not urgent; game is playable, only distant LOD in the
new city areas is affected.

## Human steps (GUI tools — cannot be run by an agent, per AGENTS.md)

Run via MO2's executable interface, in order, on `Keizaal - Fork`:

- [ ] **xLODGen** — terrain LOD, if any land was modified (city moats/walls). Output to the
      existing `xLODGEN` mod (or a `Successor`-style output mod).
- [ ] **TexGen** — regenerate LOD textures → `TexGen Output`.
- [ ] **DynDOLOD** — object LOD for the new city buildings → `DynDOLOD Output`. Use the
      same quality preset Keizaal shipped (check its DynDOLOD config/INI).
- [ ] **Grass Cache** — only if a grass-affecting mod changed; likely N/A (no grass mods
      added). Skip unless grass looks wrong.
- [ ] Confirm the regenerated outputs remain in the `Outputs` separator at highest priority.
- [ ] Post-regen: houseCARL WRLD/NAVM re-audit (worldspace change trigger, per AGENTS.md
      "Conflict re-audit practice").

## Notes

- Nemesis/behavior regen NOT needed — no behavior-affecting mods added (DAC Improved was
  omitted from the VB stack).
- Record completion date in `decisions.md` and note the last re-audit date.

## Result

(Deferred — awaiting user.)
