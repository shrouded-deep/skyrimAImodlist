---
id: task-0060
title: Tier-2 regen after content additions (xLODGen/TexGen/DynDOLOD + grass cache)
assigned_to: human
requires_housecarl: false
expected_mo2_instance: Lost Legacy - Fork
status: deferred
created: 2026-07-12
updated: 2026-07-13
depends_on: task-0072, quest-stack-review, task-0075, ui-swap
---

## Update (2026-07-13) — Retargeted to Lost Legacy

Foundation pivoted to **Lost Legacy Fork** at `D:\Skyrim\`. Dependencies updated:
must run AFTER task-0069 (city stack swap) and task-0070 (JK/Ryn re-patch) are
complete, since the city stack change is exactly what makes the existing LOD outputs
stale. Lost Legacy ships with its own DynDOLOD/TexGen/xLODGen outputs — these will
need full regeneration after the city swap.

## Description

The city stack swap (task-0069) replaces JK's Skyrim AIO with our modular city stack
(Capital Whiterun, Capital Windhelm, Ultimate Markarth, RedBag's Solitude, City of
Crossed Daggers, Riverwood Has Walls). Lost Legacy's existing LOD outputs were generated
against JK's Skyrim and will be stale after the swap.

**⚠ BLOCKING:** `DynDOLOD.esp` currently masters `JKs Skyrim.esp` which is disabled
(AIO removed in task-0069). This is a missing master — DynDOLOD regen must happen
before the list is launchable. xLODGen and TexGen can follow at list-stable time,
but DynDOLOD is urgent.

## Human steps (GUI tools — cannot be run by an agent)

Run via MO2's executable interface, in order, on `Lost Legacy - Fork`:

- [ ] **xLODGen** — terrain LOD for city wall/moat changes. Output to existing `xLODGEN`
      output mod in Lost Legacy, or create a new `Fork - xLODGen Output` mod.
- [ ] **TexGen** — regenerate LOD textures → `TexGen Output`.
- [ ] **DynDOLOD** — object LOD for new city buildings → `DynDOLOD Output`. Check Lost
      Legacy's DynDOLOD config/INI for the quality preset it shipped with and match it.
- [ ] **Grass Cache** — only if a grass-affecting mod changed; likely N/A. Skip unless
      grass looks wrong in-game.
- [ ] **Synthesis** — Lost Legacy ships with Synthesis patcher configs. Re-run Synthesis
      after city stack swap to pick up any leveled list / keyword changes from new city mods.
- [ ] Confirm regenerated outputs are at highest priority in the load order.
- [ ] Post-regen: MAST scan + houseCARL WRLD/NAVM re-audit.

## When to run

Do NOT run until ALL of the following are complete:
- task-0072 (city stack × JK interior patches)
- Quest stack review (any quest mods adding world geometry settled)
- ModEx + cheat stack added
- task-0075 (FOMOD patch pass)
- UI swap (Nordic UI out, Norden UI in — Norden FOMOD must run last so it sees the full list)
- No further additions planned that affect world geometry

The LOD regen should be the last thing before a serious playthrough, not a
checkpoint mid-build.

## Notes

- Nemesis/behavior regen NOT needed — no behavior-affecting mods added (DAC Improved was
  omitted from the VB stack).
- Record completion date in `decisions.md` and note the last re-audit date.

## Result

(Deferred — awaiting user.)
