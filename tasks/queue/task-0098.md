---
id: task-0098
title: Copy and stage second donor import wave (907 mods, graphics + missed sections)
assigned_to: cursor
requires_housecarl: false
status: queued
created: 2026-07-15
depends_on: task-0097
---

## Description

Copy 907 mod folders from the donor AE list into Lost Legacy - Fork and add them
all to modlist.txt **disabled** at the bottom as a staging area. Placement,
enabling, section assignment, and the Lost Legacy graphics section replacement all
happen in task-0075 (FOMOD + organization pass).

**Profile:** `Lost Legacy - Fork` at `D:\Skyrim\`
**MO2 must be closed before editing modlist.txt or plugins.txt.**

### Source

`D:\Skyrim AI Modlist\anvil-successor\modlist\exports\donor-ae-import-selection.json`
(updated 2026-07-15 — 907 folders across prefixes 016-094)

### Step 1 — Copy mod folders

Run `scripts/import-selected-donor-mods.ps1` with the **force/overwrite flag**
(Cursor updated the script to support this). This allows donor versions to overwrite
identically-named folders already on disk — needed for the graphics section where
donor mods may share names with existing Lost Legacy mods.

Do **not** skip collisions this run — overwrite is intentional.

### Step 2 — Strip prefixes and rename

For each copied folder: strip the `XXX.XXX ◉ ` prefix and remove `[NoDelete]`
where present, exactly as in the first import pass.

### Step 3 — Add to modlist.txt disabled

Add all 907 renamed mods to `modlist.txt` as **disabled** (`-` prefix) at the
**bottom of the list** (just above the base-game separator / list-name separator,
below all existing content). Do not create new separators. Do not enable any plugins
in plugins.txt. These are staged for task-0075 to sort out.

### Step 4 — Flag for manual review

Note the following in the Result for human review during task-0075:
- **Prefix 065 — ENB Particle Lights** (41 mods): verify these are CS-compatible
  versions before enabling. Lost Legacy uses Community Shaders, not ENB.
- **Prefix 068 — Audio Overhauls** (39 mods): larger audio stack than first import;
  check for overlap with already-active audio mods.

## Acceptance criteria

- [ ] MO2 closed before edits
- [ ] Copy script run with overwrite/force enabled
- [ ] All 907 folders renamed (prefix stripped, [NoDelete] removed)
- [ ] All 907 added to modlist.txt as `-` (disabled) at bottom of list
- [ ] No plugins added to plugins.txt
- [ ] Prefix 065 and 068 flagged in Result
- [ ] Collision count (overwritten folders) logged
- [ ] Result filled in; task moved to completed

## Result

(Filled in by Cursor.)
