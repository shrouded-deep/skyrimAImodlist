---
id: task-0098
title: Copy and install second donor import wave (907 mods — graphics replace + non-graphics staged)
assigned_to: cursor
requires_housecarl: false
status: queued
created: 2026-07-15
depends_on: task-0097
---

## Description

Copy 907 mod folders from the donor AE list into Lost Legacy - Fork. The batch
splits into two treatment groups:

- **Graphics prefixes 016–066** (~550 mods): enabled, fully replacing the existing
  Lost Legacy graphics section
- **Non-graphics prefixes 068–094** (~350 mods): added disabled at the bottom as
  a staging area for task-0075

**Profile:** `Lost Legacy - Fork` at `D:\Skyrim\`
**MO2 must be closed before editing modlist.txt or plugins.txt.**

### Source

`D:\Skyrim AI Modlist\anvil-successor\modlist\exports\donor-ae-import-selection.json`
(updated 2026-07-15 — 907 folders across prefixes 016–094)

---

## Step 1 — Copy mod folders

Run `scripts/import-selected-donor-mods.ps1` with the **force/overwrite flag**.
Donor versions overwrite identically-named existing folders — intentional for the
graphics section where donor mods may share names with LL mods.

---

## Step 2 — Strip prefixes and rename

For every copied folder: strip the `XXX.XXX ◉ ` prefix, remove `[NoDelete]` where
present. Folders whose stripped name ends in `_separator` are separator entries —
handle them accordingly in Steps 3–4.

---

## Step 3 — Graphics section replacement (prefixes 016–066, ENABLED)

### 3a — Disable existing Lost Legacy graphics mods

In `modlist.txt`, find all enabled (`+`) mod lines that sit above
`-Graphics Improvements_separator`. Disable them (change `+` → `-`). Leave the
`-Graphics Improvements_separator` line itself in place as an anchor — it will
become the bottom of the new graphics block.

### 3b — Build the donor graphics section above the existing separator

The donor graphics selection includes sub-separator entries (e.g. stripped name =
`Visual Foundations_separator`, `Mesh Fixes_separator`, etc.). For each:

1. Create the folder `D:\Skyrim\mods\<SeparatorName>\` if it doesn't exist
2. In modlist.txt, the sub-separators and their mods go **above**
   `-Graphics Improvements_separator`, in prefix-number order (016 first, 066 last)

Layout within the new graphics block (each sub-group):
```
+<mod A>
+<mod B>
-<SubSeparatorName>_separator   ← stripped folder name ending in _separator
```

Non-separator mods in each prefix group go above their group's separator entry,
in the order they appear in the selection JSON.

### 3c — Enable plugins

For any graphics mod containing `.esp`/`.esm`/`.esl` files, enable the plugin in
`plugins.txt` (prefix `*`). Place new graphics plugin entries after the existing
Fixes & Tweaks / Graphics plugin cluster.

---

## Step 4 — Non-graphics staging (prefixes 068–094, DISABLED)

Add all non-graphics renamed mods as **disabled** (`-` prefix) at the very bottom
of modlist.txt (just above the list-name / base-game separator). No new separators.
No plugins enabled. These are staged for task-0075.

Non-graphics prefixes: `068`, `069`, `072`, `073`, `076`, `077`, `078`, `079`,
`080`, `081`, `082`, `084`, `085`, `087`, `094`

---

## Step 5 — Flag for manual review (note in Result)

- **Prefix 065 — ENB Particle Lights** (41 mods): in the graphics block but needs
  verification that these are CS-compatible versions. Lost Legacy uses Community
  Shaders, not ENB. Flag each mod name so they can be reviewed before launch.
- **Prefix 068 — Audio Overhauls** (39 mods): check for overlap with already-active
  audio mods from first import.

---

## Acceptance criteria

- [ ] MO2 closed before edits
- [ ] Copy script run with overwrite/force; collision count logged
- [ ] All 907 folders renamed (prefix stripped, [NoDelete] removed)
- [ ] Existing LL graphics mods disabled in modlist.txt
- [ ] Donor graphics sub-separators created (folders + modlist entries)
- [ ] Graphics mods (016–066) added ENABLED in sub-separator structure above
      `-Graphics Improvements_separator`
- [ ] Graphics plugins enabled in plugins.txt
- [ ] Non-graphics mods (068–094) added DISABLED at bottom, no plugins enabled
- [ ] Prefix 065 mod list flagged in Result
- [ ] Result filled in; task moved to completed

## Result

(Filled in by Cursor.)
