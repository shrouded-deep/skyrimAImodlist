---
id: task-0014
title: Correct SkyUI version-check entry and update SkyUI 5.2 SE -> SkyUI 6.x
assigned_to: cursor
requires_housecarl: false
status: queued
created: 2026-07-05
depends_on:
---

## Description

`modlist/version-check-2026-07-05.md` lists SkyUI (Nexus ID 12604) as
"Current (Nexus page stale since 2017 — mod is stable)". That's now wrong
— the same Nexus page received a major community update in March 2026
("SkyUI 6", developed with doodlum), now at version 6.9+. It's the same
mod page/ID, not a fork, updated in place after 9 years of dormancy —
which is exactly why the version-check's spot-check approach missed it.

This is higher-stakes than a routine Tier 1 bump: SkyUI has near-universal
downstream dependency (many mods require or modify its menus), the
update changed the plugin to a light plugin (can reset MCM/favorites
settings on update), and it added Skyrim.esm as an explicit master. Don't
treat this as a drive-by version swap.

## Acceptance criteria

- [ ] Correct the SkyUI entry in `modlist/version-check-2026-07-05.md` —
      update installed/latest versions and remove the stale "stable since
      2017" framing
- [ ] Research what changed in SkyUI 6.x relevant to this specific list:
      aspect ratio handling (Anvil may already carry a separate
      widescreen fix — check `Complete Widescreen Fix` in the plugin
      list for overlap/redundancy), light-plugin conversion, and the new
      Skyrim.esm master requirement
- [ ] Check whether any installed mods override SkyUI menu assets and
      would need their own updates for SkyUI 6.x aspect-ratio changes to
      take effect (search plugin list / MO2 for known SkyUI-dependent
      skins/reskins)
- [ ] If it looks safe to proceed: update SkyUI in MO2, verify load order
      resolves with 0 missing masters, and note that MCM/favorites
      settings may need to be reconfigured after first launch
- [ ] If research surfaces a real compatibility risk (not just "some
      settings reset"), stop and flag it as a decision for the human
      rather than proceeding
- [ ] Update `modlist/changelog.md` and `modlist/decisions.md`

## Result

(fill in when complete)
