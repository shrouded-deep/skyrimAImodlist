---
id: task-0007
title: Individually audit Quest, Perk, MagicEffect, and Spell conflicts
assigned_to: claude-code
requires_housecarl: true
expected_mo2_instance: Anvil - Main Profile
status: queued
created: 2026-07-05
depends_on: task-0002
---

## Description

Continuing the baseline scan follow-up (task-0002): these four categories
cover quest logic and magic systems. Grouped together as the remaining
gameplay-systems set. Quest conflicts in particular can be high-risk
(script fragment / alias overrides), so don't rush this one even if the
others in the group turn out to be small.

## Acceptance criteria

- [ ] Confirm `load_order_status` reports instance `D:\Skyrim AI Modlist\
      Anvil`, profile `Anvil - Main Profile` before proceeding — stop and
      flag if it doesn't match
- [ ] Run a full line-by-line conflict audit of Quest record conflicts
- [ ] Run a full line-by-line conflict audit of Perk record conflicts
- [ ] Run a full line-by-line conflict audit of MagicEffect record
      conflicts
- [ ] Run a full line-by-line conflict audit of Spell record conflicts
- [ ] For each conflict, note the winning plugin and whether it's
      USSEP/USMP, a DLC/master override, a named compatibility patch, or
      an unresolved/unintentional conflict
- [ ] For Quest conflicts specifically, note if any involve script
      fragment or alias overrides rather than plain field overrides —
      these carry higher risk and are worth flagging even if the winner
      looks like a legitimate patch
- [ ] Clearly separate "confirmed healthy" from "needs attention" per
      category
- [ ] Append results as a new section in `modlist/baseline-scan-2026-07-05.md`
- [ ] Add a summary entry to `modlist/decisions.md`
- [ ] Audit-only — flag anything needing attention as a new follow-up
      task rather than fixing it here

## Result

(fill in when complete)
